'use client'

import { type Locale } from '@/i18n/config'

// Re-export from original with locale support
// For now, we import the original and pass locale for future i18n
export { default } from '@/app/blog/BlogContent'
